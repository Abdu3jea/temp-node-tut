const num1 = 5;
const num2 = 10;
function addValue() {
  console.log(`the sum is :${num1 + num2}`);
}
addValue();
//if we have a function inside of the module that we invoke or call yes that code run eventhough
// assigned to avarible
